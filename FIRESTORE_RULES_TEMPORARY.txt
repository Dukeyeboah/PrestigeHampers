// ⚠️ TEMPORARY FIRESTORE RULES FOR IMPORT ONLY
// Copy this section and replace the inventory rules temporarily
// REMEMBER TO RESTORE SECURE RULES AFTER IMPORT!

// Inventory collection
match /inventory/{productId} {
  // Anyone can read inventory (including unauthenticated users for browsing)
  allow read: if true;
  
  // TEMPORARY: Allow writes for import script
  // ⚠️ REMOVE THIS AFTER IMPORT!
  allow create, update, delete: if true;
}

